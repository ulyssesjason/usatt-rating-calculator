export class RatingChart {
  lower: number;
  upper: number;
  expected: number;
  upset: number;

  constructor(lower, upper, expected, upset) {
    this.lower = lower;
    this.upper = upper;
    this.expected = expected;
    this.upset;
  }
}
